; GENERATED by tools/gentables.py -- do not edit
; Suffix ladders: entering at the right slot paints an EXACT run,
; 5 cycles/byte, no loop counter, no overdraw.

        org $2C00
LADW_A
        lda #$11
        sta SCREEN+0*40,x
        sta SCREEN+95*40,x
        lda #$11
        sta SCREEN+1*40,x
        sta SCREEN+94*40,x
        lda #$11
        sta SCREEN+2*40,x
        sta SCREEN+93*40,x
        lda #$11
        sta SCREEN+3*40,x
        sta SCREEN+92*40,x
        lda #$11
        sta SCREEN+4*40,x
        sta SCREEN+91*40,x
        lda #$22
        sta SCREEN+5*40,x
        sta SCREEN+90*40,x
        lda #$22
        sta SCREEN+6*40,x
        sta SCREEN+89*40,x
        lda #$22
        sta SCREEN+7*40,x
        sta SCREEN+88*40,x
        lda #$22
        sta SCREEN+8*40,x
        sta SCREEN+87*40,x
        lda #$22
        sta SCREEN+9*40,x
        sta SCREEN+86*40,x
        lda #$33
        sta SCREEN+10*40,x
        sta SCREEN+85*40,x
        lda #$33
        sta SCREEN+11*40,x
        sta SCREEN+84*40,x
        lda #$33
        sta SCREEN+12*40,x
        sta SCREEN+83*40,x
        lda #$33
        sta SCREEN+13*40,x
        sta SCREEN+82*40,x
        lda #$33
        sta SCREEN+14*40,x
        sta SCREEN+81*40,x
        lda #$44
        sta SCREEN+15*40,x
        sta SCREEN+80*40,x
        lda #$44
        sta SCREEN+16*40,x
        sta SCREEN+79*40,x
        lda #$44
        sta SCREEN+17*40,x
        sta SCREEN+78*40,x
        lda #$44
        sta SCREEN+18*40,x
        sta SCREEN+77*40,x
        lda #$44
        sta SCREEN+19*40,x
        sta SCREEN+76*40,x
        lda #$44
        sta SCREEN+20*40,x
        sta SCREEN+75*40,x
        lda #$44
        sta SCREEN+21*40,x
        sta SCREEN+74*40,x
        lda #$44
        sta SCREEN+22*40,x
        sta SCREEN+73*40,x
        lda #$44
        sta SCREEN+23*40,x
        sta SCREEN+72*40,x
        lda #$44
        sta SCREEN+24*40,x
        sta SCREEN+71*40,x
        lda #$33
        sta SCREEN+25*40,x
        sta SCREEN+70*40,x
        lda #$33
        sta SCREEN+26*40,x
        sta SCREEN+69*40,x
        lda #$33
        sta SCREEN+27*40,x
        sta SCREEN+68*40,x
        lda #$33
        sta SCREEN+28*40,x
        sta SCREEN+67*40,x
        lda #$22
        sta SCREEN+29*40,x
        sta SCREEN+66*40,x
        lda wlum
        sta SCREEN+30*40,x
        sta SCREEN+65*40,x
        lda wlum2
        sta SCREEN+31*40,x
        sta SCREEN+64*40,x
        lda wlum
        sta SCREEN+32*40,x
        sta SCREEN+63*40,x
        lda wlum
        sta SCREEN+33*40,x
        sta SCREEN+62*40,x
        lda wlum
        sta SCREEN+34*40,x
        sta SCREEN+61*40,x
        lda wlum2
        sta SCREEN+35*40,x
        sta SCREEN+60*40,x
        lda wlum
        sta SCREEN+36*40,x
        sta SCREEN+59*40,x
        lda wlum
        sta SCREEN+37*40,x
        sta SCREEN+58*40,x
        lda wlum
        sta SCREEN+38*40,x
        sta SCREEN+57*40,x
        lda wlum2
        sta SCREEN+39*40,x
        sta SCREEN+56*40,x
        lda wlum
        sta SCREEN+40*40,x
        sta SCREEN+55*40,x
        lda wlum
        sta SCREEN+41*40,x
        sta SCREEN+54*40,x
        lda wlum
        sta SCREEN+42*40,x
        sta SCREEN+53*40,x
        lda wlum2
        sta SCREEN+43*40,x
        sta SCREEN+52*40,x
        lda wlum
        sta SCREEN+44*40,x
        sta SCREEN+51*40,x
        lda wlum
        sta SCREEN+45*40,x
        sta SCREEN+50*40,x
        lda wlum
        sta SCREEN+46*40,x
        sta SCREEN+49*40,x
        lda wlum2
        sta SCREEN+47*40,x
        sta SCREEN+48*40,x
        rts

LADR_A
        lda #$11
        sta SCREEN+47*40,x
        lda #$11
        sta SCREEN+46*40,x
        lda #$11
        sta SCREEN+45*40,x
        lda #$11
        sta SCREEN+44*40,x
        lda #$11
        sta SCREEN+43*40,x
        lda #$11
        sta SCREEN+42*40,x
        lda #$22
        sta SCREEN+41*40,x
        lda #$22
        sta SCREEN+40*40,x
        lda #$22
        sta SCREEN+39*40,x
        lda #$22
        sta SCREEN+38*40,x
        lda #$33
        sta SCREEN+37*40,x
        lda #$33
        sta SCREEN+36*40,x
        lda #$33
        sta SCREEN+35*40,x
        lda #$33
        sta SCREEN+34*40,x
        lda #$33
        sta SCREEN+33*40,x
        lda #$44
        sta SCREEN+32*40,x
        lda #$44
        sta SCREEN+31*40,x
        lda #$44
        sta SCREEN+30*40,x
        lda #$44
        sta SCREEN+29*40,x
        lda #$55
        sta SCREEN+28*40,x
        lda #$55
        sta SCREEN+27*40,x
        lda #$55
        sta SCREEN+26*40,x
        lda #$55
        sta SCREEN+25*40,x
        lda #$66
        sta SCREEN+24*40,x
        lda #$66
        sta SCREEN+23*40,x
        lda #$66
        sta SCREEN+22*40,x
        lda #$66
        sta SCREEN+21*40,x
        lda #$66
        sta SCREEN+20*40,x
        lda #$77
        sta SCREEN+19*40,x
        lda #$77
        sta SCREEN+18*40,x
        lda #$77
        sta SCREEN+17*40,x
        lda #$77
        sta SCREEN+16*40,x
        lda #$88
        sta SCREEN+15*40,x
        lda #$88
        sta SCREEN+14*40,x
        lda #$88
        sta SCREEN+13*40,x
        lda #$88
        sta SCREEN+12*40,x
        lda #$88
        sta SCREEN+11*40,x
        lda #$99
        sta SCREEN+10*40,x
        lda #$99
        sta SCREEN+9*40,x
        lda #$99
        sta SCREEN+8*40,x
        lda #$99
        sta SCREEN+7*40,x
        lda #$AA
        sta SCREEN+6*40,x
        lda #$AA
        sta SCREEN+5*40,x
        lda #$AA
        sta SCREEN+4*40,x
        lda #$AA
        sta SCREEN+3*40,x
        lda #$BB
        sta SCREEN+2*40,x
        lda #$BB
        sta SCREEN+1*40,x
        lda #$BB
        sta SCREEN+0*40,x
        rts

LADF_A
        lda #$11
        sta SCREEN+48*40,x
        lda #$11
        sta SCREEN+49*40,x
        lda #$11
        sta SCREEN+50*40,x
        lda #$11
        sta SCREEN+51*40,x
        lda #$11
        sta SCREEN+52*40,x
        lda #$22
        sta SCREEN+53*40,x
        lda #$22
        sta SCREEN+54*40,x
        lda #$22
        sta SCREEN+55*40,x
        lda #$22
        sta SCREEN+56*40,x
        lda #$33
        sta SCREEN+57*40,x
        lda #$33
        sta SCREEN+58*40,x
        lda #$33
        sta SCREEN+59*40,x
        lda #$44
        sta SCREEN+60*40,x
        lda #$44
        sta SCREEN+61*40,x
        lda #$44
        sta SCREEN+62*40,x
        lda #$44
        sta SCREEN+63*40,x
        lda #$55
        sta SCREEN+64*40,x
        lda #$55
        sta SCREEN+65*40,x
        lda #$55
        sta SCREEN+66*40,x
        lda #$55
        sta SCREEN+67*40,x
        lda #$66
        sta SCREEN+68*40,x
        lda #$66
        sta SCREEN+69*40,x
        lda #$66
        sta SCREEN+70*40,x
        lda #$66
        sta SCREEN+71*40,x
        lda #$77
        sta SCREEN+72*40,x
        lda #$77
        sta SCREEN+73*40,x
        lda #$77
        sta SCREEN+74*40,x
        lda #$88
        sta SCREEN+75*40,x
        lda #$88
        sta SCREEN+76*40,x
        lda #$88
        sta SCREEN+77*40,x
        lda #$88
        sta SCREEN+78*40,x
        lda #$99
        sta SCREEN+79*40,x
        lda #$99
        sta SCREEN+80*40,x
        lda #$99
        sta SCREEN+81*40,x
        lda #$99
        sta SCREEN+82*40,x
        lda #$AA
        sta SCREEN+83*40,x
        lda #$AA
        sta SCREEN+84*40,x
        lda #$AA
        sta SCREEN+85*40,x
        lda #$BB
        sta SCREEN+86*40,x
        lda #$BB
        sta SCREEN+87*40,x
        lda #$BB
        sta SCREEN+88*40,x
        lda #$BB
        sta SCREEN+89*40,x
        lda #$CC
        sta SCREEN+90*40,x
        lda #$CC
        sta SCREEN+91*40,x
        lda #$CC
        sta SCREEN+92*40,x
        lda #$CC
        sta SCREEN+93*40,x
        lda #$DD
        sta SCREEN+94*40,x
        lda #$DD
        sta SCREEN+95*40,x
        rts

        org $3400
LADW_B
        lda #$11
        sta SCREEN+$1000+0*40,x
        sta SCREEN+$1000+95*40,x
        lda #$11
        sta SCREEN+$1000+1*40,x
        sta SCREEN+$1000+94*40,x
        lda #$11
        sta SCREEN+$1000+2*40,x
        sta SCREEN+$1000+93*40,x
        lda #$11
        sta SCREEN+$1000+3*40,x
        sta SCREEN+$1000+92*40,x
        lda #$11
        sta SCREEN+$1000+4*40,x
        sta SCREEN+$1000+91*40,x
        lda #$22
        sta SCREEN+$1000+5*40,x
        sta SCREEN+$1000+90*40,x
        lda #$22
        sta SCREEN+$1000+6*40,x
        sta SCREEN+$1000+89*40,x
        lda #$22
        sta SCREEN+$1000+7*40,x
        sta SCREEN+$1000+88*40,x
        lda #$22
        sta SCREEN+$1000+8*40,x
        sta SCREEN+$1000+87*40,x
        lda #$22
        sta SCREEN+$1000+9*40,x
        sta SCREEN+$1000+86*40,x
        lda #$33
        sta SCREEN+$1000+10*40,x
        sta SCREEN+$1000+85*40,x
        lda #$33
        sta SCREEN+$1000+11*40,x
        sta SCREEN+$1000+84*40,x
        lda #$33
        sta SCREEN+$1000+12*40,x
        sta SCREEN+$1000+83*40,x
        lda #$33
        sta SCREEN+$1000+13*40,x
        sta SCREEN+$1000+82*40,x
        lda #$33
        sta SCREEN+$1000+14*40,x
        sta SCREEN+$1000+81*40,x
        lda #$44
        sta SCREEN+$1000+15*40,x
        sta SCREEN+$1000+80*40,x
        lda #$44
        sta SCREEN+$1000+16*40,x
        sta SCREEN+$1000+79*40,x
        lda #$44
        sta SCREEN+$1000+17*40,x
        sta SCREEN+$1000+78*40,x
        lda #$44
        sta SCREEN+$1000+18*40,x
        sta SCREEN+$1000+77*40,x
        lda #$44
        sta SCREEN+$1000+19*40,x
        sta SCREEN+$1000+76*40,x
        lda #$44
        sta SCREEN+$1000+20*40,x
        sta SCREEN+$1000+75*40,x
        lda #$44
        sta SCREEN+$1000+21*40,x
        sta SCREEN+$1000+74*40,x
        lda #$44
        sta SCREEN+$1000+22*40,x
        sta SCREEN+$1000+73*40,x
        lda #$44
        sta SCREEN+$1000+23*40,x
        sta SCREEN+$1000+72*40,x
        lda #$44
        sta SCREEN+$1000+24*40,x
        sta SCREEN+$1000+71*40,x
        lda #$33
        sta SCREEN+$1000+25*40,x
        sta SCREEN+$1000+70*40,x
        lda #$33
        sta SCREEN+$1000+26*40,x
        sta SCREEN+$1000+69*40,x
        lda #$33
        sta SCREEN+$1000+27*40,x
        sta SCREEN+$1000+68*40,x
        lda #$33
        sta SCREEN+$1000+28*40,x
        sta SCREEN+$1000+67*40,x
        lda #$22
        sta SCREEN+$1000+29*40,x
        sta SCREEN+$1000+66*40,x
        lda wlum
        sta SCREEN+$1000+30*40,x
        sta SCREEN+$1000+65*40,x
        lda wlum2
        sta SCREEN+$1000+31*40,x
        sta SCREEN+$1000+64*40,x
        lda wlum
        sta SCREEN+$1000+32*40,x
        sta SCREEN+$1000+63*40,x
        lda wlum
        sta SCREEN+$1000+33*40,x
        sta SCREEN+$1000+62*40,x
        lda wlum
        sta SCREEN+$1000+34*40,x
        sta SCREEN+$1000+61*40,x
        lda wlum2
        sta SCREEN+$1000+35*40,x
        sta SCREEN+$1000+60*40,x
        lda wlum
        sta SCREEN+$1000+36*40,x
        sta SCREEN+$1000+59*40,x
        lda wlum
        sta SCREEN+$1000+37*40,x
        sta SCREEN+$1000+58*40,x
        lda wlum
        sta SCREEN+$1000+38*40,x
        sta SCREEN+$1000+57*40,x
        lda wlum2
        sta SCREEN+$1000+39*40,x
        sta SCREEN+$1000+56*40,x
        lda wlum
        sta SCREEN+$1000+40*40,x
        sta SCREEN+$1000+55*40,x
        lda wlum
        sta SCREEN+$1000+41*40,x
        sta SCREEN+$1000+54*40,x
        lda wlum
        sta SCREEN+$1000+42*40,x
        sta SCREEN+$1000+53*40,x
        lda wlum2
        sta SCREEN+$1000+43*40,x
        sta SCREEN+$1000+52*40,x
        lda wlum
        sta SCREEN+$1000+44*40,x
        sta SCREEN+$1000+51*40,x
        lda wlum
        sta SCREEN+$1000+45*40,x
        sta SCREEN+$1000+50*40,x
        lda wlum
        sta SCREEN+$1000+46*40,x
        sta SCREEN+$1000+49*40,x
        lda wlum2
        sta SCREEN+$1000+47*40,x
        sta SCREEN+$1000+48*40,x
        rts

LADR_B
        lda #$11
        sta SCREEN+$1000+47*40,x
        lda #$11
        sta SCREEN+$1000+46*40,x
        lda #$11
        sta SCREEN+$1000+45*40,x
        lda #$11
        sta SCREEN+$1000+44*40,x
        lda #$11
        sta SCREEN+$1000+43*40,x
        lda #$11
        sta SCREEN+$1000+42*40,x
        lda #$22
        sta SCREEN+$1000+41*40,x
        lda #$22
        sta SCREEN+$1000+40*40,x
        lda #$22
        sta SCREEN+$1000+39*40,x
        lda #$22
        sta SCREEN+$1000+38*40,x
        lda #$33
        sta SCREEN+$1000+37*40,x
        lda #$33
        sta SCREEN+$1000+36*40,x
        lda #$33
        sta SCREEN+$1000+35*40,x
        lda #$33
        sta SCREEN+$1000+34*40,x
        lda #$33
        sta SCREEN+$1000+33*40,x
        lda #$44
        sta SCREEN+$1000+32*40,x
        lda #$44
        sta SCREEN+$1000+31*40,x
        lda #$44
        sta SCREEN+$1000+30*40,x
        lda #$44
        sta SCREEN+$1000+29*40,x
        lda #$55
        sta SCREEN+$1000+28*40,x
        lda #$55
        sta SCREEN+$1000+27*40,x
        lda #$55
        sta SCREEN+$1000+26*40,x
        lda #$55
        sta SCREEN+$1000+25*40,x
        lda #$66
        sta SCREEN+$1000+24*40,x
        lda #$66
        sta SCREEN+$1000+23*40,x
        lda #$66
        sta SCREEN+$1000+22*40,x
        lda #$66
        sta SCREEN+$1000+21*40,x
        lda #$66
        sta SCREEN+$1000+20*40,x
        lda #$77
        sta SCREEN+$1000+19*40,x
        lda #$77
        sta SCREEN+$1000+18*40,x
        lda #$77
        sta SCREEN+$1000+17*40,x
        lda #$77
        sta SCREEN+$1000+16*40,x
        lda #$88
        sta SCREEN+$1000+15*40,x
        lda #$88
        sta SCREEN+$1000+14*40,x
        lda #$88
        sta SCREEN+$1000+13*40,x
        lda #$88
        sta SCREEN+$1000+12*40,x
        lda #$88
        sta SCREEN+$1000+11*40,x
        lda #$99
        sta SCREEN+$1000+10*40,x
        lda #$99
        sta SCREEN+$1000+9*40,x
        lda #$99
        sta SCREEN+$1000+8*40,x
        lda #$99
        sta SCREEN+$1000+7*40,x
        lda #$AA
        sta SCREEN+$1000+6*40,x
        lda #$AA
        sta SCREEN+$1000+5*40,x
        lda #$AA
        sta SCREEN+$1000+4*40,x
        lda #$AA
        sta SCREEN+$1000+3*40,x
        lda #$BB
        sta SCREEN+$1000+2*40,x
        lda #$BB
        sta SCREEN+$1000+1*40,x
        lda #$BB
        sta SCREEN+$1000+0*40,x
        rts

LADF_B
        lda #$11
        sta SCREEN+$1000+48*40,x
        lda #$11
        sta SCREEN+$1000+49*40,x
        lda #$11
        sta SCREEN+$1000+50*40,x
        lda #$11
        sta SCREEN+$1000+51*40,x
        lda #$11
        sta SCREEN+$1000+52*40,x
        lda #$22
        sta SCREEN+$1000+53*40,x
        lda #$22
        sta SCREEN+$1000+54*40,x
        lda #$22
        sta SCREEN+$1000+55*40,x
        lda #$22
        sta SCREEN+$1000+56*40,x
        lda #$33
        sta SCREEN+$1000+57*40,x
        lda #$33
        sta SCREEN+$1000+58*40,x
        lda #$33
        sta SCREEN+$1000+59*40,x
        lda #$44
        sta SCREEN+$1000+60*40,x
        lda #$44
        sta SCREEN+$1000+61*40,x
        lda #$44
        sta SCREEN+$1000+62*40,x
        lda #$44
        sta SCREEN+$1000+63*40,x
        lda #$55
        sta SCREEN+$1000+64*40,x
        lda #$55
        sta SCREEN+$1000+65*40,x
        lda #$55
        sta SCREEN+$1000+66*40,x
        lda #$55
        sta SCREEN+$1000+67*40,x
        lda #$66
        sta SCREEN+$1000+68*40,x
        lda #$66
        sta SCREEN+$1000+69*40,x
        lda #$66
        sta SCREEN+$1000+70*40,x
        lda #$66
        sta SCREEN+$1000+71*40,x
        lda #$77
        sta SCREEN+$1000+72*40,x
        lda #$77
        sta SCREEN+$1000+73*40,x
        lda #$77
        sta SCREEN+$1000+74*40,x
        lda #$88
        sta SCREEN+$1000+75*40,x
        lda #$88
        sta SCREEN+$1000+76*40,x
        lda #$88
        sta SCREEN+$1000+77*40,x
        lda #$88
        sta SCREEN+$1000+78*40,x
        lda #$99
        sta SCREEN+$1000+79*40,x
        lda #$99
        sta SCREEN+$1000+80*40,x
        lda #$99
        sta SCREEN+$1000+81*40,x
        lda #$99
        sta SCREEN+$1000+82*40,x
        lda #$AA
        sta SCREEN+$1000+83*40,x
        lda #$AA
        sta SCREEN+$1000+84*40,x
        lda #$AA
        sta SCREEN+$1000+85*40,x
        lda #$BB
        sta SCREEN+$1000+86*40,x
        lda #$BB
        sta SCREEN+$1000+87*40,x
        lda #$BB
        sta SCREEN+$1000+88*40,x
        lda #$BB
        sta SCREEN+$1000+89*40,x
        lda #$CC
        sta SCREEN+$1000+90*40,x
        lda #$CC
        sta SCREEN+$1000+91*40,x
        lda #$CC
        sta SCREEN+$1000+92*40,x
        lda #$CC
        sta SCREEN+$1000+93*40,x
        lda #$DD
        sta SCREEN+$1000+94*40,x
        lda #$DD
        sta SCREEN+$1000+95*40,x
        rts
